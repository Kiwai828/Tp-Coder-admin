name: tp_coder
description: TP Coder - AI-Powered Code Builder
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  http: ^1.2.0
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  socket_io_client: ^2.0.3+1
  url_launcher: ^6.2.4
  intl: ^0.20.2
  google_sign_in: ^6.2.1
  image_picker: ^1.0.7
  file_picker: ^8.0.0
  webview_flutter: ^4.7.0
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
